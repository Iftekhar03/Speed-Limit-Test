#include <stdio.h>

int main(void) {
	int t,n,x,y,z,count,i,j,a,b,c;
	scanf("%d",&t);
	while(t--)
	{
	  scanf("%d %d %d %d",&a,&x,&b,&y);
	  if(a/x==b/y)
      printf("\nEqual\n");
    else if(a/x>b/y)
      printf("\nAlice\n");
    else
      printf("\nBob\n");
	}
	return 0;
}
